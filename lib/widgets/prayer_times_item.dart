

import 'package:flutter/cupertino.dart';

class PrayerTimesItem extends StatelessWidget {
  const PrayerTimesItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}
